# Dependency info

Lists information summary for each 1-st level dependency of the project

## Installation

```shell
npm i -g deps-info
```

## Usage

```shell
deps-info > deps.json
```

or

```shell
deps-info --csv > deps.csv
```